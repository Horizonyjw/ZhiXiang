# FNO 接口测试配置（已对齐 radar_v1）
# 真实 handoff 联调请优先使用：configs/20260814-fno-radar_v1-01.yaml

experiment_id: "20260809-fno-placeholder-01"
model_name: fno
data_version: radar_v1
notes: "默认接口已对齐 radar_v1；随机张量结果不得当作正式训练结论"

# ---- radar_v1 统一接口 ----
tin: 5
tout: 3
in_channels: 1
height: 352
width: 512
physical_unit: normalized_grayscale_proxy_NOT_dBZ

# ---- FNO 结构（接口测试默认值）----
# 注意：隐藏通道数勿与空间 width 字段重名
fno_width: 32
modes1: 16
modes2: 16
n_layers: 4

# ---- 运行 ----
seed: 42
device: auto    # auto | cuda | cpu
