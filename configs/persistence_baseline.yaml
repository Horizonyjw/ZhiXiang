# Persistence 基线配置（已对齐 radar_v1）
# 真实 handoff 联调请优先使用：configs/20260814-persistence-radar_v1-01.yaml

experiment_id: "20260809-persistence-placeholder-01"
model_name: persistence
data_version: radar_v1
notes: "默认接口已对齐 radar_v1；随机张量结果不得当作正式联调结论"

# ---- radar_v1 统一接口 ----
tin: 5
tout: 3
in_channels: 1
height: 352
width: 512
physical_unit: normalized_grayscale_proxy_NOT_dBZ

# ---- 运行 ----
seed: 42
batch_size: 2
device: auto    # auto | cuda | cpu

# ---- 结果保存 ----
output_dir: "results/20260809-persistence-placeholder-01"
