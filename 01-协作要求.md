01-协作要求

1.  文件用途

本文件是所有项目成员开始数据、模型、评测或智能体任务前必须阅读的统一说明。目的是避免出现环境不同、数据维度不一致、实验结果无法追踪、代码互相覆盖和数据违规使用等问题。

所有成员在首次提交代码或结果前，应完成文末的"开始前检查清单"。如本文件与后续已确认的README、环境文件或接口说明冲突，以仓库中最新且经过组内确认的版本为准，并记录变更原因。

2.  开始前必须明确的项目边界

当前任务是雷达回波短临预测和自主实验优化的原型验证，不是正式气象预报系统。

第一优先级是形成可运行、可复现的实验闭环，不是一次堆叠多个复杂模型。

所有模型必须使用统一的数据划分和评测程序，不能各自选择有利样本或修改指标口径。

论文和展示材料只能使用已经运行并保存证据的结果，不能把计划写成已完成成果。

航空运行风险解释属于后续扩展，预测流程未稳定前不优先开展。

3.  统一开发环境要求

3.1 基本原则

项目开发语言暂定为Python
3.10及以上，最终版本以仓库中的environment.yml、requirements.txt或等效环境文件为准。

PyTorch和CUDA版本由统一环境负责人确认。其他成员不要自行更换主要版本后直接提交代码。

不要在代码中写死个人电脑路径、用户名、盘符或GPU编号。

不要把API密钥、账号密码、访问令牌或服务器登录信息写入代码、配置文件和共享文档。

环境存在问题时，先记录报错命令、完整报错和当前版本，再请求协助，不要只发送报错截图。

3.2 每个人必须完成的环境验证

至少完成并记录：

Python版本；

PyTorch版本；

CUDA是否可用；

当前GPU型号和可用显存；

能否完成一次PyTorch张量运算；

能否运行项目提供的最小测试命令。

环境未统一前，可以阅读代码和做独立调研，但不要形成另一套无法合并的长期开发环境。

4.  统一代码目录

项目代码原则上放入以下目录：

点击图片可查看完整电子表格

新增目录或调整结构前先与总体联调负责人确认，不要为个人代码重复建立另一套项目结构。

5.  统一数据接口

第一阶段统一采用：

模型输入：\[B, Tin, C, H, W\]

模型输出：\[B, Tout, C, H, W\]

默认数据类型：float32

其中：

B：批量大小；

Tin：历史输入帧数；

Tout：未来预测帧数；

C：通道数；

H、W：空间高度和宽度。

如果U-Net需要将时间维合并到通道维，应在模型内部转换；DataLoader和评测程序的公共格式不随模型变化。

DataLoader至少应返回：

历史输入序列；

真实未来序列；

能够追踪样本时间或来源的必要元信息。

任何归一化、裁剪、缺失值填充、单位转换和掩码处理都必须写入数据说明或配置文件，不能只存在于个人代码中。

6.  数据使用与安全要求

任何数据在标记为"可用"前，必须完成实际获取、代码读取和绘图验证。

数据负责人必须记录来源、发布机构、获取方式、许可和引用要求。

未确认授权的本地雷达数据不得上传至公共仓库、公开网盘或第三方平台。

原始大规模数据、模型权重和大量预测结果原则上不提交到Git仓库。

训练集、验证集和测试集优先按天气过程划分，避免同一过程同时出现在训练集和测试集。

发现缺帧、全零帧、异常值、损坏文件或尺寸不一致时，记录样本编号和处理方法，不要静默删除。

7.  Git与协作要求

不直接在主分支上长期开发；按模块建立清晰的功能分支。

分支名称应能说明用途，例如feature/data-loader、feature/unet-baseline、feature/evaluation。

每次提交只完成一个相对明确的改动，提交说明写清做了什么。

合并前先同步最新代码并解决冲突，不使用强制推送覆盖其他成员工作。

不提交个人路径、临时缓存、数据集、模型权重、密钥和无关的大文件。

修改公共接口、目录结构或配置字段前，必须在群内说明，并同步更新README或接口说明。

8.  统一实验记录要求

每次需要保留的实验至少记录：

实验编号；

日期和负责人；

Git提交或代码版本；

数据版本和划分；

输入帧数、预测帧数和时间间隔；

模型及主要参数；

随机种子；

运行环境和GPU；

训练时长和推理时长；

MAE或MSE、CSI、POD、FAR等结果；

配置、权重、日志、预测文件和结果图位置；

是否成功、失败原因及下一步处理。

推荐实验编号格式：

YYYYMMDD-模型-数据版本-序号

例如：20260809-unet-data_v01-01。

只有结果截图、没有配置和日志的实验，不能作为正式对比结果。失败实验同样需要保留原因，避免其他成员重复踩坑。

9.  模型开发的最低检查顺序

任何新模型接入项目时，按以下顺序进行：

随机张量前向传播；

检查输入输出维度；

使用一批真实样本前向传播；

使用8～16个样本进行过拟合测试；

小规模数据训练；

正式训练和统一评测。

如果模型无法通过小样本过拟合测试，不直接开展长时间训练，应优先检查数据、损失函数、学习率和模型输出。

10. 评测要求

所有模型使用同一套测试数据、阈值和评测脚本。

CSI、POD和FAR的回波阈值写入配置文件，不得在个人代码中随意修改。

需要同时保留总体指标和不同预测步长指标。

指标计算前检查形状、单位、归一化范围和缺失值。

评测程序应先使用人工数组验证，再用于真实模型结果。

不只汇报表现最好的样本，应保留典型成功和失败案例。

11. 半周同步要求

每周三晚和周日晚同步。同步前，每位成员必须更新：

本阶段已完成任务；

实际产物位置；

未完成内容及原因；

当前阻塞和需要的协作；

下一阶段计划。

技术任务的完成应以代码、数据、日志、指标或结果图为依据。只阅读资料、只有口头说明或只有截图，不视为完成可交接的技术任务。

如果公共接口发生变化，应在同步会上明确：修改内容、影响成员、兼容方式和生效时间。

12. 遇到以下情况必须先停止并确认

不清楚数据单位或数值含义；

不确定缺失值和无回波区域的区别；

准备更改公共输入输出维度；

准备更换主要Python、PyTorch或CUDA版本；

准备重新划分训练、验证和测试数据；

准备修改统一指标、回波阈值或评价口径；

准备上传可能受限制的本地雷达数据；

准备让智能体自动修改公共代码或启动高成本实验。

以上事项未经确认不要自行假设，否则可能使已有实验失去可比性。

13. 开始前检查清单

每位成员开始任务前确认：

已阅读《00-项目整体目标与阶段规划》；

已阅读本文件；

已获得项目代码和共享材料访问权限；

已按照统一环境完成最小测试；

已确认自己负责的工作模块和本阶段交付；

已确认当前数据接口和结果保存方式；

已确认本阶段需要向谁获取输入、向谁交付结果；

已建立个人功能分支；

已确认不在代码或文档中泄露密钥及受限数据；

已知道周三、周日同步前需要更新哪些内容。
